"@martian-engineering/lossless-claw": patch
---

Wait for deferred LCM database initialization after lock-contended gateway restarts, and surface the real retry failure when deferred startup cannot recover.
